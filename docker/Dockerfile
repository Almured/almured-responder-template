# Stub — fleshed out in W5c.
#
# Final image will:
#   - Use a pinned slim-bookworm Python 3.11 base
#   - Run as a non-root user
#   - Copy src/ and data/ into the image
#   - Expose the responder agent's MCP transport port
#   - Set sane resource defaults
#
# Until W5c, this file exists so the repo layout is complete.

FROM python:3.11-slim-bookworm
WORKDIR /app
CMD ["echo", "almured-responder-template: Dockerfile not yet implemented (W5c)"]
